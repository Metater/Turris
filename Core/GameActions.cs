using UnityEngine;

public static class GameActions
{
    public static void SpawnEntity(ushort entityId, EntityType entityType, Vector3 spawnPosition)
    {
        //GameManager.I.entityManager.SpawnEntity(entityId, entityType, spawnPosition);
    }

    public static void DespawnEntity(ushort entityId)
    {
        //GameManager.I.entityManager.DespawnEntity(entityId);
    }
}
